import { Typography } from '@mui/material';

const NotFoundPage = () => {
  return (
    <Typography sx={{ textAlign: 'center', py: 10 }} variant="h4" component="p">
      Page not found.
    </Typography>
  );
};

export default NotFoundPage;
